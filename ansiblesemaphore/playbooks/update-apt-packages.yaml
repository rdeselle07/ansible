---
- hosts: all

  become: true
  become_method: sudo
  become_user: root
  tasks:
    - name: Update Apt Packages
      apt:
        update_cache: yes
        upgrade: yes
        cache_valid_time: 86400
